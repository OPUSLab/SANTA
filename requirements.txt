# Core Python dependencies for the S²ANTA repository.
# Install CUDA-enabled torch separately before installing this file.
#
# Tested torch stack:
#   python -m pip install --index-url https://download.pytorch.org/whl/cu128 torch==2.10.0+cu128
#
# Tested FlashAttention install:
#   python -m pip install flash-attn==2.7.4.post1 --no-build-isolation
#
# Optional legacy/reference backend:
#   python -m pip install flashinfer-python==0.6.6

transformers==4.57.0
accelerate==1.13.0
numpy==2.2.6
ninja==1.13.0
packaging==26.0
setuptools==82.0.1
wheel==0.46.3
huggingface-hub==0.36.2
tokenizers==0.22.2
safetensors==0.7.0