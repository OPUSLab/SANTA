# Install CUDA-enabled torch separately for the local CUDA version before using this file.
# Example only:
# python -m pip install torch --index-url https://download.pytorch.org/whl/cu128

transformers>=4.52.4
accelerate>=1.0.0
numpy>=1.23
ninja>=1.11
packaging>=23.0
setuptools>=68.0
wheel>=0.42

# Install FlashAttention separately because it often needs --no-build-isolation:
# python -m pip install flash-attn==2.7.4.post1 --no-build-isolation

# Optional legacy/reference backend, not required for the main FA2/S²ANTA commands:
# flashinfer-python==0.5.3
